;
(function(){
'use strict';

sliderController.$inject = ['$scope', 'dataservice', '$http', '$window', '$document'];

function sliderController ($scope, dataservice, $http, $window, $document) {
			
			
};

angular
		.module('app')
		.controller('sliderController', sliderController)
})();